# E-Shop Core API 

![CI Status](https://github.com/MaxymDmytruk/OnlineShopProject/actions/workflows/ci.yml/badge.svg)


Онлайн-магазину цифрових товарів (E-Shop).


## 📂 Звіт по Лабораторних

### ✅ Практична 1: Базова архітектура
* **Завдання:** Створити простий сервер, БД та клієнт.
* **Результат:** Реалізовано зв'язку Flask + SQLite + HTML.
* **Файли:**
    * [app.py](./app.py) — Точка входу.
    * [init_db.py](./init_db.py) — Ініціалізація БД.

### ✅ Практична 2: Документація та Roadmap
* **Завдання:** Описати бізнес-вимоги та архітектуру.
* **Результат:** Створено папку `docs/` з повним описом системи.
* **Артефакти:**
    * 📄 [Бізнес-документ](./docs/business_doc.md) (Roles, Functional/Non-functional requirements).
    * 🗺️ [Архітектурна діаграма](./docs/architecture_diagram.png).
    * 📅 [Roadmap проєкту](./docs/roadmap.md).

### ✅ Практична 3: Domain-Driven Design (DDD)
* **Завдання:** Виділити бізнес-контексти та сутності.
* **Результат:** Виділено контексти `Users`, `Catalog`, `Orders`.
* **Артефакти:**
    * [Глосарій та Сутності](./docs/domain/entities.md).
    * [Контекстна мапа](./docs/domain/context_map.png).
    * 📂 [Код моделей (domain/models.py)](./domain/models.py).

### ✅ Практична 4: REST API, OpenAPI & CRUD
* **Завдання:** Спроєктувати API та реалізувати CRUD.
* **Архітектурний стиль:** Модульний Моноліт ([ADR-0001](./docs/adr/0001-architecture-style.md)).
* **Результат:** Реалізовано повний CRUD для Товарів та Авторизацію.
* **Артефакти:**
    * 📄 **[Контракт API (openapi.yaml)](./docs/api/openapi.yaml)**.
    * 📸 [Скріншот Swagger Editor](./docs/api/swagger_screenshot.png).
    * 📂 Код: [api/product_routes.py](./api/product_routes.py) та [service/product_service.py](./service/product_service.py).

### ✅ Практична 5: Error Handling & Reliability
* **Завдання:** Реалізувати ідемпотентність, rate-limiting, ретраї.
* **Результат:** Сервер та клієнт стійкі до збоїв.
* **Реалізовано:**
    * **Idempotency:** Заголовок `Idempotency-Key` захищає від дублів при створенні товару.
    * **Rate Limiting:** Обмеження кількості запитів з одного IP (повертає 429).
    * **Smart Client:** JS автоматично робить ретраї при помилках мережі.
    * **Degraded Mode:** Жовтий банер на сайті при проблемах зі з'єднанням.

### ✅ Практична 6: Docker & Containerization
* **Завдання:** Контейнеризувати застосунок.
* **Результат:** Проєкт запускається однією командою.
* **Артефакти:**
    * 🐳 [Dockerfile](./Dockerfile) (Multistage build, Python 3.10 slim).
    * 🐙 [docker-compose.yml](./docker-compose.yml) (Сервіс API + Volume для БД).
    * Файл [.dockerignore](./.dockerignore) для оптимізації контексту.


### 💻 Як запустити проєкт локально

Щоб запустити проєкт на своєму комп'ютері (Вимога Практичної 6):

### Варіант 1: Через Docker Compose (Рекомендовано)
Це підніме все оточення однією командою.

1.  **Створіть файл `.env`** у корені проєкту:
    ```bash
    SECRET_KEY=dev-secret-key
    ```
2.  **Запустіть:**
    ```bash
    docker compose up --build
    ```
3.  **Відкрийте:** http://127.0.0.1:5000

### Варіант 2: Вручну (Python)
```bash
# 1. Встановіть залежності
pip install -r requirements.txt

# 2. Ініціалізуйте БД (створить адміна)
python init_db.py

# 3. Запустіть сервер
python app.py
```

## 🛠️ Практична 7: CI/CD 

У цьому проєкті налаштовано повний **CI/CD пайплайн** за допомогою GitHub Actions.

### 1. CI (Continuous Integration)
При кожному `push` у гілку `main` або `pull request`:
* ✅ **Setup:** Налаштовується оточення Python 3.10.
* ✅ **Install:** Встановлюються залежності з `requirements.txt`.
* ✅ **Test:** Запускаються автоматичні тести через `pytest`.

### 2. CD (Continuous Delivery)
Якщо тести пройшли успішно (і це `push` у `main`):
* 📦 **Build:** Збирається Docker-образ застосунку.
* 🚀 **Publish:** Образ автоматично публікується в **GHCR** (GitHub Container Registry).

**Адреса образу:**
`ghcr.io/maxymdmytruk/shop-api:latest`

### ⭐ Style Points (Виконані бонусні вимоги)
Ми реалізували додаткові покращення пайплайну:
1.  🔒 **Trivy Security Scan:** Образ автоматично сканується на вразливості (CVE) перед публікацією.
2.  📝 **Job Summary:** Після виконання генерується гарний Markdown-звіт у підсумках (Summary) з результатами тестів та посиланням на образ.



## Практична робота 8: Observability з Grafana, Loki, Tempo та OpenTelemetry


### RED Dashboard (Grafana)

Після входу у Grafana (admin/admin) відкрито дашборд:
Observability Demo → RED

📸 Скріншот 1 — RED dashboard

![img.png](img.png)

✏ Пояснення:

На RED-дашборді відображається  ключові метрики для сервісу demo-app:

Rate — кількість HTTP-запитів у секунду;

Errors — кількість помилок (5xx);

Duration — середня/перцентильна тривалість запиту.

Це дозволяє оцінити швидкість, стабільність і затримки сервісу.

### Tempo — Трейси
📸 Скріншот 2 — відкритий Trace у Tempo

![img_1.png](img_1.png)

✏ Пояснення:

На цьому скрині видно трейс одного HTTP-запиту, який проходив через сервіс demo-app.
Показано:

тривалість кожного спану,

залежності між компонентами,

локальні теги запиту.

Це дозволяє аналізувати затримки та визначати вузькі місця.

##  Loki — Логи
📸 Скріншот 3 — логи в Grafana Explore / Loki

![img_2.png](img_2.png)

✏ Пояснення:

На скрині видно логи сервісу demo-app, які збираються в Loki.
Тут можна фільтрувати логи за рівнем (info, error), часом і сервісом.

#  Власний RED Dashboard

Було створено власний дашборд у Grafana:
My Demo RED

Усі 4 панелі реагують на зміну глобального time range.

## Панелі дашборду
### 1. Requests Per Second (RPS)

PromQL:

sum by (service_name) (rate(http_requests_total[1m])) 


Пояснення:
Графік показує кількість запитів за секунду для demo-app.
Корисно для контролю навантаження і просідань.

### 2. Error Rate (%)

PromQL:

100 *
(
  sum(rate(http_requests_error_total[1m]))
  /
  sum(rate(http_requests_total[1m]))
)


Пояснення:
Показує відсоток помилок від загальної кількості запитів (5xx).
Важливо для моніторингу стабільності.

### 3. Latency (p95)

PromQL:

histogram_quantile(
  0.95, 
  rate(http_request_duration_ms_bucket[5m])
)


Пояснення:
Показує 95-й перцентиль затримки.
Це метрика, що важлива у продакшені для SLA/SLO.

## Скріншоти власного дашборду

![img_4.png](img_4.png)

## JSON дашборду
📄 [Завантажити JSON дашборду](./docs/my-demo-red-dashboard.json)


